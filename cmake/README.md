# CMake Support Modules

This directory is reserved for build-system helpers and shared CMake fragments.

Current deterministic build entrypoints:

- root `CMakeLists.txt`
- `CMakePresets.json`
- toolchain bootstrap scripts in `tools/`
