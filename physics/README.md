# Physics Module Map

Current physics/simulation implementations live in:

- `src/domain/env/`
- `sim/`

This top-level directory exists as a stable module anchor for future high-fidelity physics extraction without breaking public CLI interfaces.
