# Inference Module Map

Runtime inference implementations live in:

- `src/domain/inference/`

This top-level directory exists as a stable module anchor for future extraction of inference-only build targets.
